package com.saborhub.domain.entities.usuario;

public class UsuarioFactory {
    private Usuario usuario;

    public Usuario incluiEndereco(String rua, String numero, String cidade, String estado, String cep) {
        // this.usuario.setEndereco(new Endereco(rua, numero, cidade, estado, cep));

        return this.usuario;
    }

}